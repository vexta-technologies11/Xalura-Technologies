# Generated markdown logs & reports (keep folders via .gitkeep)
logs/**/*.md
reports/**/*.md

# Persisted cycle counters (local / server — do not commit live state)
data/cycle-state.json

# Append-only inter-department queue (Phase 5)
shared/event-queue.log

# Failed operations log (Phase 6)
failed/operations-queue.json
