export const departmentId = "publishing" as const;

export function describePublishingDepartment(): string {
  return "Publishing — articles, courses (Worker → Manager → Executive pipeline in pipeline.ts)";
}
